
- name: "Install Claude"
  import_tasks: claude/claude.yml
  when: install_claude
  tags:
    - claude

- name: "Install OpenCode"
  import_tasks: opencode.yml
  when: install_opencode
  tags:
    - opencode
    - opencode_cli
    - open_code
    - open_code_cli
