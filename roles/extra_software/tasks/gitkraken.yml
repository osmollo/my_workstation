---

- include_vars:
    file: "{{ ansible_distribution | lower }}.yml"

- import_tasks: gitkraken_ubuntu.yml
  when: ansible_distribution == "Ubuntu"

- import_tasks: gitkraken_fedora.yml
  when: ansible_distribution == "Fedora"
