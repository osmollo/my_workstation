---

- name: "[FEDORA] Copy repository file"
  copy:
    src: spotify.repo
    dest: /etc/yum.repos.d/fedora-spotify.repo
    mode: 0644
    owner: root
    group: root

- name: "[FEDORA] Install Spotify"
  yum:
    name: spotify-client
    state: present
    update_cache: true
