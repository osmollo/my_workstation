---

- include_vars:
    file: "{{ ansible_distribution | lower }}.yml"

- import_tasks: teamviewer_fedora.yml
  when: ansible_distribution == "Fedora"

- import_tasks: teamviewer_ubuntu.yml
  when: ansible_distribution == "Ubuntu"
