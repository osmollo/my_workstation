---

- block:
    - name: "[MEGASYNC] [ARCH] Install megasync"
      become: false
      yay:
        name: "{{ appimage_launcher_package }}"
        state: present
        update_cache: true
      when: linux_distribution == "arch"

  tags:
    - appimage_launcher

- block:
  tags:
    - appimage_update
