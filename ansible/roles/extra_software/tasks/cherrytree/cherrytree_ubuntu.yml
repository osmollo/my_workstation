---

- name: "[UBUNTU] install cherrytree repository"
  apt_repository:
    repo: ppa:giuspen/ppa
    state: present
    codename: "{{ ubuntu_codename }}"

- name: "[UBUNTU] install cherrytree package"
  apt:
    name: "{{ cherrytree_package }}"
    state: latest
    update_cache: true
