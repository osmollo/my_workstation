---

- name: "[ARCH] Install virtualbox"
  become: false
  yay:
    name: "{{ virtualbox_package }}"
    state: present
    update_cache: true

- name: "[ARCH] Install vagrant"
  become: false
  yay:
    name: "{{ vagrant_package }}"
    state: present
    update_cache: true
  tags:
    - install_vagrant
