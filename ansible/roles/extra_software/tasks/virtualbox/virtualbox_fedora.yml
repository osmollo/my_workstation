---

- name: "[FEDORA] Import repository key"
  rpm_key:
    key: https://www.virtualbox.org/download/oracle_vbox.asc
    state: present

- name: "[FEDORA] Create repository file"
  get_url:
    url: https://download.virtualbox.org/virtualbox/rpm/fedora/virtualbox.repo
    dest: /etc/yum.repos.d/virtualbox.repo

- name: "[FEDORA] Install Virtualbox packages"
  yum:
    name: "{{ virtualbox_packages }}"
    state: latest
    update_cache: true

- name: "[FEDORA] Install Vagrant"
  yum:
    name: "{{ vagrant_package }}"
    state: latest
    update_cache: true
  tags:
    - vagrant

- name: "[FEDORA] Uninstall vagrant-libvirt if is installed"
  yum:
    name: vagrant-libvirt
    state: absent
  tags:
    - vagrant
